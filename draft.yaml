models:
  # User:
  #   name: string
  #   email: string
  #   web_url: text nullable
  #   address: text nullable
  #   phone_number: string nullable
  #   postal_code: string nullable
  #   logo_url: string
  #   password: string

  CommonLetterLog:
    user_id: id foreign:users.id onUpdate:cascade onDelete:cascade
    title: string
    type: string

  LetterLog:
    common_letter_log_id: id foreign:common_letter_logs onUpdate:cascade onDelete:cascade
    field_name: string
    field_value: text
    # number_of_letter: string nullable # masuk ke letter log saja
    # destination: string # masuk ke letter log saja

seeders: CommonLetterLog, LetterLog
